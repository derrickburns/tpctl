apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: glooe-grafana
    chart: grafana-4.0.1
    heritage: Helm
    release: glooe
  name: glooe-grafana
  namespace: gloo-system
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi

---
apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: proxy-client-secret
  namespace: sso
secretDescriptor:
  backendType: secretsManager
  data:
  - key: qa1/sso/proxy-client-secret
    name: proxy-client-secret
    property: proxy-client-secret

